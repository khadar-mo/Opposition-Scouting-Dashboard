[project]
name = "scout-backend"
version = "0.1.0"
description = "FastAPI read-only API serving precomputed opposition-scouting metrics"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "psycopg[binary,pool]>=3.2",
]

[dependency-groups]
dev = ["pytest>=8", "httpx>=0.27", "ruff", "mypy"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/backend"]
